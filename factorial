#include <stdio.h>
int main()
{
    int num,fact=1,i=1;
    printf("\tEnter a positive number:");
    scanf("%d",&num);
    do
    {
        fact=fact*i;
        i++;
    }while(i<=num);
    printf("\t The factorial of %d is %d",num,fact);

    return 0;
}
